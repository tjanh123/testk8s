server:
  port: 8200
spring:
  application:
    name: k8s-test2
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${K8S_SERVICE_DISCOVERY_HOST}:8761/eureka/
